#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Validando proyecto antes de push..."

npx -y pnpm@9 install --frozen-lockfile || exit 1
npx -y tsc --noEmit || exit 1

echo "✅ Todo correcto. Push permitido."
