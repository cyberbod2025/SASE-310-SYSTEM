#!/usr/bin/env sh
set -e

# Skip pre-commit hooks in CI or when explicitly disabled
if [ "$CI" = "true" ] || [ "$SKIP_PRECOMMIT" = "true" ]; then
  echo "CI detected or SKIP_PRECOMMIT=true, skipping pre-commit checks."
  exit 0
fi

pnpm test
